# Webhook to Google Ads offline conversion

A dependency-free Node example that normalizes CRM and checkout webhooks into a stable conversion event. It demonstrates source ID selection, click ID allowlisting, revenue normalization, and deterministic deduplication before an ad-platform client is called.
